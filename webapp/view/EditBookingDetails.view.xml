<mvc:View
    controllerName="sap.ui.com.project1.controller.EditBookingDetails"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:layout="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
>
    <Page
        showHeader="false"
        enableScrolling="true"
    >
        <ScrollContainer>
            <core:Fragment
                fragmentName="sap.ui.com.project1.fragment.Header"
                type="XML"
            />
            <VBox class="sapUiMediumMarginBottom">
                <Panel
                    headerText="{i18n>generalInformation}"
                    expandable="false"
                    class="customPanelHeader"
                >
                    <HBox
                        justifyContent="End"
                        width="100%"
                    >
                        <Button
                            id="idEditBtn"
                            text="{i18n>MSedit}"
                            tooltip="{i18n>MSedit}"
                            press="onPressEditSave"
                            class="myUnifiedBtn"
                        />
                        <Button
                            text="{i18n>CancelButton}"
                            tooltip="{i18n>CancelButton}"
                            press="onPressCancelBooking"
                            class="myUnifiedBtn"
                        />
                    </HBox>

                    <f:SimpleForm
                        id="idGeneralForm"
                        editable="true"
                        layout="ColumnLayout"
                        columnsM="3"
                        columnsL="4"
                        columnsXL="6"
                    >
                        <f:content>
                            <!-- Booking Details -->
                            <core:Title text="{i18n>bookingDetailsTitle}" />
                            <Label
                                text="{i18n>roomtype}"
                                required="true"
                            />
                            <Text
                                text="{HostelModel>/RoomType}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <ComboBox
                                id="idBedTypeCombo"
                                visible="{viewModel>/editMode}"
                                selectedKey="{HostelModel>/RoomType}"
                                items="{ path: 'HostelModel>/', templateShareable: false}"
                                selectionChange="onSelectionChange"
                            >
                                <core:Item
                                    key="{HostelModel>Name}"
                                    text="{HostelModel>Name}"
                                />
                            </ComboBox>

                            <Label
                                text="{i18n>placePrice}"
                                required="true"
                            />
                            <Text text="{HostelModel>/RoomPrice}" />

                            <Label
                                text="{i18n>paymentMethod}"
                                required="true"
                            />
                            <Text
                                text="{HostelModel>/PaymentType}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <ComboBox
                                value="{HostelModel>/PaymentType}"
                                visible="{viewModel>/editMode}"
                                selectedKey="{HostelModel>/PaymentType}"
                                items="{ path: 'BedTypeModel>/', templateShareable: false }"
                            >
                                <core:ListItem
                                    key="Monthly"
                                    text="Per Month"
                                />
                                <core:ListItem
                                    key="Daily"
                                    text="Per Day"
                                />
                            </ComboBox>
                            <!-- Project Address -->

                            <!-- <Label text="No of Persons" required="true" />
                <Text text="{HostelModel>/noofperson}" visible="true"/> -->
                            <!-- <Input value="{HostelModel>/noofperson}" visible="{viewModel>/editMode}"/> -->

                            <!-- Site Contact -->
                            <core:Title text="{i18n>dates}" />
                            <Label
                                text="{i18n>startDate}"
                                required="true"
                            />
                            <Text
                                text="{HostelModel>/StartDate}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <DatePicker
                                value="{HostelModel>/StartDate}"
                                visible="{viewModel>/editMode}"
                                valueFormat="dd/MM/yyyy"
                                displayFormat="dd/MM/yyyy"
                                change="onFieldValidation"
                                width="100%"
                            />

                            <Label
                                text="{i18n>endDate}"
                                required="true"
                            />
                            <Text
                                text="{HostelModel>/EndDate}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <DatePicker
                                value="{HostelModel>/EndDate}"
                                visible="{viewModel>/editMode}"
                                valueFormat="dd/MM/yyyy"
                                displayFormat="dd/MM/yyyy"
                                change="onFieldValidation"
                                width="100%"
                            />
                        </f:content>
                    </f:SimpleForm>
                </Panel>
            </VBox>
            <VBox>
                <Panel
                    headerText="{i18n>personalInformation} "
                    expandable="false"
                    class="customPanelHeader"
                >
                    <f:SimpleForm
                        id="idPersonalForm"
                        editable="true"
                        layout="ColumnLayout"
                        columnsM="1"
                        columnsL="2"
                        columnsXL="2"
                        labelSpanXL="4"
                        labelSpanL="3"
                        labelSpanM="4"
                        labelSpanS="12"
                    >
                        <f:content>
                            <core:Title text="" />
                            <Label text="{i18n>fullname}" />
                            <Text
                                text="{HostelModel>/FullName}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/FullName}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>MVDDOB}" />
                            <Text
                                text="{path:'HostelModel>/DateOfBirth',  formatter: '.Formatter.formatDate'}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <DatePicker
                                value="{path:'HostelModel>/DateOfBirth',  formatter: '.Formatter.formatDate'}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>MVgender}" />
                            <Text
                                text="{HostelModel>/Gender}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/Gender}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>MSmobileNo}" />
                            <Text
                                text="{HostelModel>/MobileNo}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/MobileNo}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>email}" />
                            <Text
                                text="{HostelModel>/CustomerEmail}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/CustomerEmail}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>MVDcountry}" />
                            <Text
                                text="{HostelModel>/Country}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/Country}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>MVDstate}" />
                            <Text
                                text="{HostelModel>/State}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/State}"
                                visible="{viewModel>/editMode}"
                            />

                            <Label text="{i18n>city}" />
                            <Text
                                text="{HostelModel>/City}"
                                visible="{= !${viewModel>/editMode} }"
                            />
                            <Input
                                value="{HostelModel>/City}"
                                visible="{viewModel>/editMode}"
                            />
                        </f:content>
                    </f:SimpleForm>

                    <Table
                        id="idFacilityRoomTableDetails"
                        class="sapUiSmallMarginTop"
                        items="{ path: 'HostelModel>/AllSelectedFacilities'}"
                        mode="SingleSelectLeft"
                        inset="false"
                        selectionChange="onTableSelection"
                    >
                        <headerToolbar>
                            <Toolbar>
                                <Title
                                    text="{i18n>bookingDetailsTitle}"
                                    level="H5"
                                />
                                <ToolbarSpacer />
                                <Button
                                    icon=""
                                    id="id_addfacility"
                                    text="{i18n>addFacility}"
                                    tooltip="{i18n>addFacility}"
                                    press="onPressAddFaciliti"
                                    class="myUnifiedBtn"
                                />
                                <Button
                                    icon="sap-icon://edit"
                                    text="{i18n>MSedit}"
                                    tooltip="{i18n>editFacility}"
                                    press="onEditFacilityDetails"
                                    class="myUnifiedBtn"
                                    id="id_editfacility"
                                />
                            </Toolbar>
                        </headerToolbar>

                        <columns>
                            <Column>
                                <Text text="{i18n>facilityname}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>faciltyprice}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>startDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>endDate}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>Tooldays}" />
                            </Column>
                            <Column>
                                <Text text="{i18n>total}" />
                            </Column>
                        </columns>

                        <items>
                            <ColumnListItem>
                                <cells>
                                    <Text text="{HostelModel>FacilityName}" />
                                    <Text text="{HostelModel>Price}" />
                                    <Text
                                        text="{ path: 'HostelModel>StartDate'}"
                                    />
                                    <Text
                                        text="{ path: 'HostelModel>EndDate', formatter: '.Formatter.formatDate'}"
                                    />
                                    <Text text="{HostelModel>TotalDays}" />
                                    <Text
                                        text="{=(${HostelModel>Price} * ${HostelModel>TotalDays}).toFixed(2) }"
                                    />
                                </cells>
                            </ColumnListItem>
                        </items>
                    </Table>
                    <VBox class="sapUiSmallMarginTop">
                        <HBox
                            justifyContent="End"
                            class="sapUiSmallMarginTop"
                        >
                            <Label
                                text="{i18n>totalfacilitycost}"
                                design="Bold"
                                class="sapUiSmallMarginEnd"
                            />
                            <Text
                                text="{ path: 'HostelModel>/TotalFacilityPrice', formatter: '.Formatter.formatCurrency'}"
                                class="sapUiSmallMarginEnd"
                            />
                        </HBox>
                        <HBox
                            justifyContent="End"
                            class="sapUiSmallMarginTop"
                        >
                            <Label
                                text="{i18n>roomRent}"
                                design="Bold"
                                class="sapUiSmallMarginEnd"
                            />
                            <Text
                                text="{HostelModel>/RoomPrice}"
                                class="sapUiSmallMarginEnd"
                            />
                        </HBox>
                        <HBox
                            justifyContent="End"
                            class="sapUiSmallMarginTop"
                        >
                            <Label
                                text="{i18n>grandTotal}"
                                design="Bold"
                                class="sapUiSmallMarginEnd"
                            />
                            <Text
                                text="{ path: 'HostelModel>/GrandTotal',formatter: '.Formatter.formatCurrency'  }"
                                class="sapUiSmallMarginEnd"
                            />
                        </HBox>
                    </VBox>
                </Panel>
            </VBox>
        </ScrollContainer>
        <footer>
            <OverflowToolbar>
                <ToolbarSpacer />
                <Button
                    text="{i18n>proceedToPay}"
                    visible="false"
                    tooltip="{i18n>proceedToPay}"
                    class="myUnifiedBtn"
                    press="onOpenProceedtoPay"
                />
            </OverflowToolbar>
        </footer>
    </Page>
</mvc:View>
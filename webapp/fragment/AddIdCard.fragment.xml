<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:f="sap.ui.layout.form"
	xmlns:core="sap.ui.core"
	xmlns:u="sap.ui.unified">
	<Dialog title="{i18n>addCardDetails}">
		<MessageStrip
            text="{i18n>imageMsg}"
            type="Warning"
            showIcon="true"
            class="sapUiTinyMarginBottom" />
		<f:SimpleForm
            editable="true"
            layout="ResponsiveGridLayout"
            labelSpanXL="3"
            labelSpanL="3"
            labelSpanM="3"
            labelSpanS="12"
            adjustLabelSpan="false"
            emptySpanXL="4"
            emptySpanL="4"
            emptySpanM="4"
            emptySpanS="0"
            columnsXL="1"
            columnsL="1"
            columnsM="1"
            singleContainerFullSize="false">
			<f:content>
				<Label text="Employee Type" required="true"/>
				<RadioButtonGroup id="idGroupC"  selectedIndex="0" select="onRadioButtonSelect"  class="center-content" columns="2" editable="{IdCardModel>/isEditable}">
					<RadioButton text="{i18n>employeeOffer}" />
					<RadioButton text="{i18n>contractOffer}" />
				</RadioButtonGroup>
				<!-- Employee ID -->
				<Label text="{i18n>employeeID}"  required="true" />
				<ComboBox
    id="comboBox"
    showSecondaryValues="true"
    value="{IdCardModel>/EmployeeID}" editable="{IdCardModel>/isEditable}" 
    change=".onEmployeeIDChange">
					<items>
						<core:ListItem
            key="{IdCardModel>key}"
            text="{IdCardModel>key}"
            additionalText="{IdCardModel>text}" />
					</items>
				</ComboBox>
				<!-- Employee Name -->
				<Label text="{i18n>employeeName}" required="true" />
				<Input value="{IdCardModel>/EmployeeName}" liveChange="validateName" maxLength="50" valueStateText="{i18n>validEmpNameMsg}" id="IC_id_EmployeeName" editable="{IdCardModel>/isEditable}" />
				<!-- Designation -->
				<Label text="{i18n>designation}" required="true"/>
				<Input value="{IdCardModel>/Designation}" liveChange="ValidateCommonFields" maxLength="20" id="IC_id_Designation" valueStateText="{i18n>destinationValueState}"/>
				<!-- Email -->
				<Label text="{i18n>emailId}" required="true"/>
				<Input value="{IdCardModel>/Email}" id="IC_id_Email" liveChange="validateEmail" maxLength="50" valueStateText="{i18n>emailValueState}"/>
				<!-- DOB -->
				<Label text="{i18n>dateOfBirth}" required="true" />
				<DatePicker displayFormat="dd/MM/yyyy" valueFormat="dd/MM/yyyy" value="{IdCardModel>/DOB}" id="IC_id_DateBirth" maxDate="{IdCardModel>/today}" initialFocusedDateValue="{IdCardModel>/minDOB}" change="validateDate" valueStateText="{i18n>dateValueState}" />
				<!-- MobileNo -->
				<Label text="{i18n>mobileNo}" required="true"/>
				<Input value="{IdCardModel>/MobileNo}" id="IC_id_Mobile" liveChange="validateMobileNo" maxLength="10" valueStateText="{i18n>mobileNoValueState}" />
				<!-- BloodGroup -->
				<Label text="{i18n>bloodGroup}" required="true" />
				<ComboBox showSecondaryValues="true" id="IC_id_bloodGroup"  items="{BloodGroup>/bloodgroup}" change="ValidateCommonFields" valueStateText="{i18n>validBloodGroupMsg}" value="{IdCardModel>/BloodGroup}" >
					<core:Item key="{BloodGroup>ID}" text="{BloodGroup>Code}" />
				</ComboBox>
				<!-- File Uploader -->
				<Label text="{i18n>fileUploader}" required="true"/>
				<HBox width="100%" justifyContent="Start" alignItems="Center">
					<VBox width="30%">
						<u:FileUploader                       
                        name="myFileUpload"
                        uploadUrl="upload/"
                        tooltip="{i18n>fileUploadId}"
                        change="IC_onHandleUploadPress"
                        buttonOnly="true"  
						id="idfile"  
						fileType="jpg,png,jpeg"  
						valueStateText="{i18n>fileUploaderValueState}"               
                        uploadComplete="handleUploadComplete"/>
					</VBox>
					<VBox width="70%" >
						<Text text="{TextDisplay>/name}"></Text>
					</VBox>
				</HBox>
				<Label text="{i18n>picture}"/>
				<Button text="{i18n>openCamera}" icon="sap-icon://camera" press="IC_onPressOpenCamera" />
				<Label/>
				<VBox id="canvasContainer" visible="true" class="sapUiSmallMargin canvas">
					<core:HTML content="&lt;canvas id='canvas' class='passport-size-canvas'&gt;&lt;/canvas&gt;" />
				</VBox>
			</f:content>
		</f:SimpleForm>
		<!-- Buttons for Submit and Close actions -->
		<FlexBox justifyContent="End" width="100%">
			<Button text="{i18n>submit}" type="Emphasized" press="IC_onPressSubmit" class="sendMail" visible="{IdCardModel>/Submit}"/>
			<Button text="{i18n>save}" type="Emphasized" press="IC_onPressSave" class="sendMail" visible="{IdCardModel>/Save}"/>
			<Button text="{i18n>close}" class="sendMail" press="IC_onPressClose" />
		</FlexBox>
	</Dialog>
</core:FragmentDefinition>
